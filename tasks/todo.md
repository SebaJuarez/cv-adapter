# TODO — Generalización del motor híbrido de recuperación

- [ ] Fase 1: stemming ES/EN en `tokenize_with_synonyms` + `use_stemming` + tests
  - Aceptación: `pytest` verde; variantes morfológicas matchean; `ci/cd`/`c++` intactos; ATS literal intacto
  - Verificar: `pytest tests/test_retrieval.py`
  - Archivos: `src/retrieval/sparse.py`, `src/config.py`, `tests/test_retrieval.py`
- [ ] Fase 2: modelos multilingües (e5-small + mMARCO) + prefijos E5 + fix invalidación de índice
  - Aceptación: `is_fresh` invalida al cambiar modelo/stemming; prefijos `query:`/`passage:` solo con e5; `pytest` verde
  - Verificar: `pytest tests/test_retrieval.py tests/test_selection.py`
  - Archivos: `src/config.py`, `src/retrieval/dense.py`, `src/retrieval/store.py`, `src/selection.py`, tests
- [ ] Fase 3: `rrf_k` + `sparse_weight`/`dense_weight` configurables
  - Aceptación: RRF respeta k y pesos; defaults documentados; `pytest` verde
  - Verificar: `pytest tests/test_retrieval.py`
  - Archivos: `src/retrieval/hybrid.py`, `src/config.py`, `src/selection.py`, tests
- [ ] Fase 4: eval harness + eval set sintético
  - Aceptación: `python scripts/eval_retrieval.py` corre con el set sintético y reporta métricas + barrido
  - Verificar: `python scripts/eval_retrieval.py`
  - Archivos: `tests/data/eval/*.json`, `scripts/eval_retrieval.py`, `data/eval/README.md` (gitignored data)
- [ ] Fase 5: verificación integral
  - Aceptación: `pytest` completo verde; CLI con JD real OK; barrido define defaults finales
  - Verificar: `pytest; python scripts/eval_retrieval.py; python main.py ...`
  - Archivos: `tasks/`, `AGENTS.md` si aplica, `src/config.py` (defaults finales)
