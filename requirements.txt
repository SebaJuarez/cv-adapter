# --- Frontend web local ---
fastapi>=0.115.0
uvicorn>=0.32.0

# --- Cliente Ollama (LLM local) ---
# Requiere Ollama >= 0.5 instalado en el sistema (no es un paquete pip) para
# soportar Structured Outputs (parámetro `format` con JSON Schema).
# Descarga: https://ollama.com/download
ollama>=0.4.4

# --- Cliente LLM remoto (API compatible con OpenAI) ---
# Se usa cuando config["llm_provider"] = "openai" (OpenAI, OpenRouter, Groq…).
# No requiere servidor local; necesita una API key en config.json.
openai>=1.40.0

# --- YAML ---
PyYAML>=6.0.2

# --- Renderizado CV YAML -> PDF ---
rendercv[full]>=2.8

# --- Importación de CVs: extracción de texto de PDFs viejos ---
pypdf>=6.0.0

# --- Motor de Information Retrieval  ---
# BM25 léxico + sinónimos (Python puro)
rank-bm25>=0.2.2

# Stemming Snowball ES/EN para el tokenizador BM25 (sin descargas de datos)
nltk>=3.9.0

# Embeddings densos locales (CPU-only)
# Probado hasta 5.x (5.6.1); el techo <6.0 evita cortes breaking futuros.
sentence-transformers>=3.0.0,<6.0

# Álgebra lineal para similitud coseno
numpy>=1.26.0,<3.0

# Framework de deep learning (carga de cross-encoders, backend CPU)
# Probado hasta 2.x (2.13.0); el techo <3.0 evita cortes breaking futuros.
torch>=2.0.0,<3.0

# Transformers (dependencia transitiva de sentence-transformers; el código
# no lo importa directo). Probado hasta 5.x (5.14.1); techo <6.0.
transformers>=4.35.0,<6.0

# --- Utilidades de consola (opcional, mejora los prints) ---
rich>=13.9.4