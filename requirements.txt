# --- Orquestación ---
langgraph>=0.2.60
langchain-core>=0.3.28

# --- Frontend web local ---
fastapi>=0.115.0
uvicorn>=0.32.0

# --- Cliente Ollama (LLM local) ---
# Requiere Ollama >= 0.5 instalado en el sistema (no es un paquete pip) para
# soportar Structured Outputs (parámetro `format` con JSON Schema).
# Descarga: https://ollama.com/download
ollama>=0.4.4

# --- YAML ---
PyYAML>=6.0.2

# --- Renderizado CV YAML -> PDF ---
# OJO: el extra [full] es obligatorio, si instalás solo "rendercv" el CLI
# te va a pedir que reinstales con [full] (trae el motor Typst embebido).
rendercv[full]>=2.8

# --- Utilidades de consola (opcional, mejora los prints) ---
rich>=13.9.4